# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
*.egg
.eggs/
build/
dist/
.tox/
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pyright_cache/

# --- Virtualenvs ---
.venv/
venv/
env/
ENV/

# --- Aion runtime / secrets ---
# .env-active is generated by scripts/build.py and DOES get committed
# (it's brand-configuration output, not secrets). .env-secret holds
# API keys and MUST NOT ship.
.env-secret
.env-secret.*
.env.local

# --- Editor / OS junk ---
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# --- Test scratch ---
/tmp-test-*/

# --- Strategy / business plan (local-only) ---
# The selling plan stays on disk for execution; never ships to the public
# repo. Public pricing and projections create pressure and competitive intel.
docs/SELLING_PLAN.md
