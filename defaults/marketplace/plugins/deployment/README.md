# deployment

Ship code. Three skills covering the recurring shape of releasing any service-like project.

| Skill | Triggered by |
|---|---|
| `dockerfile-craft` | "write a Dockerfile", "containerize this", "Docker image" |
| `ci-configuration` | "set up CI", "GitHub Actions for X", slow/flaky CI questions |
| `release-tagging` | "cut a release", "bump version", "tag a release" |
