---
description: Run a full diagnostic health-check on the install — config, plugins, marketplaces, memory git, hooks, MCP servers, authentication, PATH.
---

Dispatch the doctor agent to run a full health-check on this install. If the user passes a focus area in $ARGUMENTS, scope the check there; otherwise do the full sweep.
